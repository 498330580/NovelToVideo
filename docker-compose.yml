version: '3.8'

services:
  novel-to-video:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./output:/app/output
      - ./temp:/app/temp
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - FLASK_ENV=production